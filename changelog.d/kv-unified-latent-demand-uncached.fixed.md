- kv-unified latent demand is ranked on every compile again. The ranking cache added earlier in this cycle was keyed on the summary roots and budget only, so it replayed stale demand after appended messages or policy changes, shared one slot with the production-budget shadow pick, and survived branch switches. The ranking has no candidates, and costs nothing, while fewer than `mergeThreshold` same-level summaries line up.
